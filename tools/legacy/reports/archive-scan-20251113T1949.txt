归档扫描报告
生成时间: 2025-11-13T19:49:00+08:00
作者: automation (由助理生成)

概述
- 本报告记录对 `tools/` 目录中生成产物、临时脚本和快照文件的引用检查与归档操作。
- 已将明确为一次性或临时用途且未被其它脚本引用的文件移动到 `tools/legacy/` 并将相应路径加入 `.gitignore`。

一、已归档文件（移动到 `tools/legacy/`）
- tools/legacy/consistency-report.json
- tools/legacy/low-risk-format-fixes.normalized.patch
- tools/legacy/markdown-md013-suggestions.patch
- tools/legacy/markdownlint-project-raw.txt
- tools/legacy/markdownlint-project.json
- tools/legacy/markdownlint-summary.txt
- tools/legacy/tmp_check_dedupe.py
- tools/legacy/_long_lines_scan.py

说明: 上述文件为 lint/报告快照、补丁或临时扫描脚本。移动操作保留了文件历史（git 重命名），并已将相应模式追加到仓库根 `.gitignore`，以避免未来误提交生成产物。

二、被跳过 / 保留（有引用，未归档）
- tools/placeholder_whitelist.txt
  - 原因: 被 CI/workflow 与脚本引用（例如 `.github/workflows/links-and-inventory.yml`, `.github/workflows/organizer.yml`, `tools/check_placeholders.py`, `tools/run_archive_candidates.ps1`），属于运行时或 pipeline 所需资源，不能归档。

三、扫描与操作记录（摘要）
- 搜索方式: 对 `tools/` 下的常见生成产物扩展名（`.patch`, `.txt`, `.json`, 另外也检查 `*.bak/*.log`）执行仓库引用检查（`git grep`），仅在零引用时将文件归档。
- 归档策略: 归档到 `tools/legacy/`（保留历史），并把对应文件路径追加到 `.gitignore`（已提交）。
- 提交信息（最近一次归档提交）:
  - 提交: f21bb82 2025-11-13 19:48:37 +0800 Tiger-00700
  - 说明: chore(tools): archive generated reports/patches to tools/legacy and ignore them
  - 变更: 多个文件从 `tools/` 重命名到 `tools/legacy/`，并修改 `.gitignore`。

四、后续建议
- 若需要进一步收窄归档策略，可指定额外的扩展名或路径（例如 `tools/reports/` 下所有时间戳快照、`examples/99_book_exports/` 导出产物等）。
- 归档操作为可逆（保留历史），若某脚本运行报告缺失，可通过 `git mv tools/legacy/<file> tools/` 恢复并重新生成。

报告结束。
