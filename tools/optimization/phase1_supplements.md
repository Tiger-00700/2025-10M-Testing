**Phase1 Supplements**

- **Status**: Completed full read of `book/1022.2025.newbook.cleaned.md` (36,802 lines). This document lists Phase1 actionable suggestions derived from the book and prior extraction/smoke results.

- **Goal**: Turn book code blocks into a maintainable, runnable `examples/第5篇` set and prepare a conservative remediation plan that minimizes risk while enabling review and incremental fixes.

- **1. Extract & canonicalize runnable examples**: Create one runnable script per self-contained code block and place under `examples/第5篇/<chapter>/`.
  - Impact: High; Complexity: Medium

- **2. Prioritize high-value examples**: Mark examples by priority (High: demonstrative/complete; Medium: small fixes; Low: fragments/tests) and focus repair effort on High first.
  - Impact: High; Complexity: Low

- **3. Preserve fragments as SKIPPED with context**: Keep pytest/test fragments and fixture-dependent blocks but mark them `SKIPPED` in `README-smoke.md` with a short reason and referenced fixture names.
  - Impact: Medium; Complexity: Low

- **4. Add minimal fixture stubs (optional, targeted)**: For top-priority pytest fragments, optionally add small fixture stubs or harness wrappers to allow smoke verification without changing semantics.
  - Impact: Medium; Complexity: Medium

- **5. Create per-example metadata files**: For each generated example produce a small YAML/JSON with keys: `source_location` (book line/section), `priority`, `status` (extracted|manual-fix|required-deps), `runtime` (py/java), `requirements`.
  - Impact: High; Complexity: Low

- **6. Consolidate dependencies**: Generate per-folder `requirements.txt` (Python) and a top-level `examples/requirements-review.txt` listing frequently used third-party packages for CI reproducibility.
  - Impact: High; Complexity: Low

- **7. CI smoke job**: Add a dedicated CI job `examples-smoke` that: checks out branch, creates environment (python:3.11), installs `examples` requirements, runs `tools/smoke_run_examples.py`, uploads `smoke_report.txt` and logs as artifacts.
  - Impact: High; Complexity: Medium

- **8. Conservative deletion policy for `tools/`**: Do NOT delete tools automatically. Instead produce `tools/removable-candidates.txt` listing tools that appear unused (based on recent runs and imports) and require manual approval per-item.
  - Impact: High (safety); Complexity: Low

- **9. Handle large cleanup commit**: Move the large cleanup commit off `main` if uncertain:
  - Option 1 (safe): create a branch `cleanup-snapshot` preserving the commit and revert `main` to prior state.
  - Option 2: keep commit on `main` if you confirm it was intended. (+Add tag `cleanup-2025-10`)
  - Impact: High; Complexity: Medium

- **10. Repair plan & small-fix PRs**: For each High-priority example, open small focused PRs (one PR per chapter or per 5 files) that contain minimal, well-documented fixes and updated `requirements` if needed.
  - Impact: High; Complexity: Medium

- **11. README & reviewer checklist**: Update `examples/第5篇/README-smoke.md` to include: how to run smoke locally, what SKIPPED means, priority list, and a small reviewer checklist (verify, run, accept, merge).
  - Impact: Medium; Complexity: Low

- **12. Automation improvements**: Enhance `tools/smoke_run_examples.py` to (a) better classify fragments (pytest vs code), (b) auto-extract imports for `requirements.txt` hints, and (c) tag examples that need external systems (kafka/db) as `EXTERNAL`.
  - Impact: Medium; Complexity: Medium

- **13. Delivery & PR packaging**: Prepare a delivery branch `examples/phase1` containing:
  - extracted `examples/第5篇/` candidate files
  - `README-smoke.md` and `extraction_report.txt`
  - `tools/optimization/phase1_supplements.md` (this file)
  - small-fix commits for top-priority files
  - Impact: High; Complexity: Low

- **Next immediate steps (what I can do now if you approve)**:
  1. Create branch `examples/phase1` and commit the candidate examples + `README-smoke.md`.
  2. Add CI smoke job draft and run one CI-local smoke to collect artifacts.
  3. Produce `tools/removable-candidates.txt` listing `tools/` files that appear safe-to-remove (for manual review).

- **Decisions requested**:
  - Q1: Keep the large cleanup commit on `main` or move it to `cleanup-snapshot` branch? (Recommend: move to branch unless you explicitly confirm keeping it.)
  - Q2: Pytest fragments remediation: Option A: attempt automated fixture stubs for high-priority items; Option B (recommended): leave as SKIPPED and fix manually per-priority. Which do you prefer?

- **Notes / constraints**:
  - I will not delete any `tools/` files or remove commits without explicit approval.
  - Phase1 deliverables will be committed to a new branch (not `main`) unless you ask otherwise.

---

Generated by automation after full-book read. Next: create branch + commit candidates if you approve the two decisions above.
