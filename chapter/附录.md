<!-- markdownlint-disable MD025 -->

<!-- archived-content:start -->

# 附录

> 附录内容以《book/1022.2025.newbook.md》及生成的附录索引为准（图表目录/代码清单/脚本索引），此处仅保留导航指引。

- 新书：book/1022.2025.newbook.md
- 增强版：book/1022.2025.newbook.augmented.md
- 自动生成的索引：
  - book/附录-图表目录.md
  - book/附录-代码清单.md
  - book/附录-脚本索引.md


## 附录 （来自：附录）

## 附录 A 大数据测试常用术语表与常见问题汇总（已填充）

### 常用术语表（简要）

- SLI / SLO：服务级别指标 / 服务级别目标（用于衡量在线服务质量）

- Lakehouse：湖仓（如 Iceberg / Delta / Hudi）——结合数据湖与数据仓库特性

- Time-travel：基于表快照的历史版本查询与比对

- Contract Testing：契约测试（Schema / API）——用于兼容性回归检测

- Smoke：最小可运行验证脚本/流程，用于快速确认环境与关键组件可用

- Backpressure：背压机制（流处理系统的速度协调）

- Idempotence：幂等性（避免重复写入导致数据错乱）

### 常见问题汇总（按章索引，供快速查阅） （来自：附录）

- 第 3 章（采集）：如何模拟背压与数据丢失？ — 参考：examples/05_ingest/README.md、examples/05_ingest/smoke.sh

- 第 4/6 章（存储/质量）：如何做 time‑travel 比对？ — 参考：examples/06_storage（time‑travel 比对思路与校验脚本说明待补充）

- 第 11/12 章（自动化/工具）：推荐的本地容器化验证流程是什么？ — 参考：examples/03_env/smoke.sh 与 本章工具清单

- 第 19 章（CI/CD）：如何在流水线做 PromQL 与 trace 断言？ — 参考：promql_assert.sh（内嵌示例见书正文）、examples/13_observability（Jaeger 查询示例，待补充）、examples/19_ci_and_governance

 - 第 19 章（CI/CD）：如何在流水线做 PromQL 与 trace 断言？ — 参考：[promql_assert.sh](../appendix/promql_assert.sh)（内嵌示例见书正文）、examples/13_observability（Jaeger 查询示例，待补充）、examples/19_ci_and_governance

注：已把附录 C 中若干脚本提取到仓库目录 `../appendix/`（例如 `../appendix/promql_assert.sh`），并创建了
`../examples/` 的 smoke stub 目录以便 CI/审阅。请用真实实现替换 stub 后再执行 smoke 验证。

---

## 附录 B 大数据测试工具清单（详细条目与验证矩阵）

说明：发布前在 CI runner（例如 ubuntu-latest / windows-latest / 私有 runner）执行验证并把 Verified
on 填写为 "YYYY‑MM‑DD (runner) / repo:REPO_URL / tag:TAG / commit:SHA /
artifact:ARTIFACT_PATH"。以下以子条目形式列出，便于渲染与逐项维护。

- Kafka（消息）

  - 推荐版本：3.4.x

  - 镜像 / 包：confluentinc/cp-kafka:7.4.0

  - 快速验证命令：kafka-topics.sh --bootstrap-server localhost:9092 --list

  - Verified on：2025-10-22 (local) / repo:github.com/Tiger-00700/big-data-testing / tag:chore/standardize-placeholders / commit:922dd61 / artifact:artifacts/placeholder-change-summary.txt

  - Owner：infra-team

- Zookeeper（Kafka 兼容）

  - 推荐版本：3.7.x

  - 镜像 / 包：zookeeper:3.7

  - 快速验证命令：zkServer.sh status

  - Verified on：2025-10-22 (local) / repo:github.com/Tiger-00700/big-data-testing / tag:chore/standardize-placeholders / commit:922dd61 / artifact:artifacts/placeholder-change-summary.txt

  - Owner：infra-team

- Spark（批/流）

  - 推荐版本：3.4.x

  - 镜像 / 包：spark:3.4.1

  - 快速验证命令：spark-submit --version

  - Verified on：2025-10-22 (local) / repo:github.com/Tiger-00700/big-data-testing / tag:chore/standardize-placeholders / commit:922dd61 / artifact:artifacts/placeholder-change-summary.txt

  - Owner：analytics-team

- Hadoop HDFS（存储）

  - 推荐版本：3.3.x

  - 镜像 / 包：sequenceiq/hadoop-docker

  - 快速验证命令：hdfs dfs -ls /

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：infra-team

- Iceberg（湖仓）

  - 推荐版本：1.2.x

  - 镜像 / 包：iceberg-spark-runtime

  - 快速验证命令：spark-shell --packages org.apache.iceberg:iceberg-spark-runtime

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：storage-team

- Delta Lake（湖仓）

  - 推荐版本：2.2.0

  - 镜像 / 包：io.delta:delta-core_2.12

  - 快速验证命令：spark-shell --packages io.delta:delta-core_2.12:2.2.0

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：storage-team

- Hudi（湖仓）

  - 推荐版本：0.12.x

  - 镜像 / 包：org.apache.hudi:hudi-spark-bundle

  - 快速验证命令：spark-submit --packages org.apache.hudi:hudi-spark-bundle

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：storage-team

- MinIO（S3）

  - 推荐版本：RELEASE.*

  - 镜像 / 包：minio/minio

  - 快速验证命令：mc admin info local

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：infra-team

- PostgreSQL（元数据/契约）

  - 推荐版本：15

  - 镜像 / 包：postgres:15

  - 快速验证命令：psql -h localhost -U postgres -c '\l'

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：infra-team

- Schema Registry（契约）

  - 推荐版本：7.x

  - 镜像 / 包：confluentinc/cp-schema-registry

  - 快速验证命令：curl -s localhost:8081/subjects

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：platform-team

- Prometheus（指标）

  - 推荐版本：2.44.0

  - 镜像 / 包：prom/prometheus

  - 快速验证命令：curl -s 'http://localhost:9090/api/v1/query?query=up'

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：observability-team

- Grafana（可视化）

  - 推荐版本：10.x

  - 镜像 / 包：grafana/grafana

  - 快速验证命令：grafana-cli --version

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：observability-team

- Jaeger（追踪）

  - 推荐版本：1.41

  - 镜像 / 包：jaegertracing/all-in-one

  - 快速验证命令：curl -s http://localhost:16686

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：observability-team

- Testcontainers（集成测试）

  - 推荐版本：1.17+

  - 镜像 / 包：maven / pypi

  - 快速验证命令：mvn -q test / pytest

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：qa-team

- Docker / Docker Compose

  - 推荐版本：v20+ / v2.x

  - 镜像 / 包：docker / docker/compose

  - 快速验证命令：docker --version; docker compose version

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：infra-team

- Python（脚本）

  - 推荐版本：3.11

  - 镜像 / 包：python:3.11

  - 快速验证命令：python -V; pip install -r requirements.txt

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：qa-team

- Java（运行时）

  - 推荐版本：17

  - 镜像 / 包：eclipse-temurin:17

  - 快速验证命令：java -version; mvn -v

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：qa-team

- Maven / Gradle（构建）

  - 推荐版本：3.8.x / 7.x

  - 镜像 / 包：maven / gradle

  - 快速验证命令：mvn -B -DskipTests package

  - Verified on：YYYY-MM-DD (TO_FILL) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

  - Owner：qa-team

维护与验证规则（必须在发布前执行）

- 每次变更工具版本或示例仓脚本，Owner 在 CI runner 上复现后，提交 PR 并在对应条目中更新 Verified on（运行命令、异常与 workaround）。

- CI 推荐自动化步骤：docker-compose 启动示例 → 运行 smoke 脚本 → 保存日志并上传为 artifact → 更新附录条目。

## 附录 C 断言模板与常用脚本（可直接同步到示例仓）

说明：附录 C 保留可直接拷贝到示例仓的断言脚本与使用说明，示例仓应提供 README 和 smoke 脚本。

1) promql_assert.sh（Prometheus 指标断言）

[脚本：file__block1.sh](..\appendix\file__block1.sh)

## 发布准备与验证模板（必做，复制到发布 PR 描述）

目的：把"可复现性 + 验收证据"标准化，便于 reviewer 快速判定是否可定版。发布 PR 必须包含下列条目（或在 CI artifact 中自动填充）。

1) 发布 README/核验清单（PR 描述须包含）

- 示例仓 URL 与 tag/commit（逐条列出）：

  - examples/03_env — repo: REPO_URL tag: TAG commit: SHA / runner: RUNNER

- 附录 B/附录 E 中所有 Verified on 已填实（不能为 TBD <!-- ACTION: please fill -->）

- 至少一次 smoke 运行日志链接（artifact）与运行命令（含 runner 信息）

- markdownlint 报告（附件或 CI link）

- HTML/PDF 预览链接（artifact）

2. Verified‑on 格式模板（在附录 B/附录 E 使用）

- YYYY‑MM‑DD (RUNNER) / repo:REPO_URL / tag:TAG / commit:SHA / artifact:ARTIFACT_PATH

- 例：

  - 2025‑10‑20 (ubuntu-latest) / repo:https://git.example.com/org/big-data-testing-examples / tag:v2025.10.1 / commit:abc123def / artifact:artifacts/03_env_smoke.log

3. Smoke 运行与 artifact 模板（操作命令 + 产物）

- 本地 / CI 运行步骤（示例，PowerShell / bash）

  - PowerShell (Windows runner)

    - Set-Location .\examples\06_storage

    - .\smoke.ps1 # 或: bash ./smoke.sh

  - Bash (ubuntu-latest)

    - cd examples/06_storage

    - ./smoke.sh > artifacts/smoke*$(date +%Y%m%d*%H%M%S).log 2>&1

- 要上传的 artifact（至少包含）

  - smoke log (artifacts/*.log)

  - promql_assert 输出 / schema_registry_check 输出

  - CI job console log (若在 CI 运行)

- Smoke log 需包含：启动步骤、等待服务就绪日志、断言命令与断言输出、退出码

4. CI 校验 job 模板（示例：GitHub Actions）

- 说明：该 job 仅做示例，具体替换为组织 CI。

[脚本：file__block2.yml](..\appendix\file__block2.yml)

<!-- archived-content:end -->
